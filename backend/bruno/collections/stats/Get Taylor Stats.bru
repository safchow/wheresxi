meta {
  name: Get Taylor Stats
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/stats/taylor
  body: none
  auth: none
}

headers {
  Origin: {{client_url}}
}

params:query {
}

script:pre-request {
  const token = bru.getEnvVar("auth_token") || bru.getVar("auth_token");

  if (token) {
    req.setHeader("Authorization", `Bearer ${token}`);
  }
}

docs {
  # Get Taylor Stats

  Return Taylor dossier stats and recent arrivals.

  **Endpoint:** `GET /api/stats/taylor`
}

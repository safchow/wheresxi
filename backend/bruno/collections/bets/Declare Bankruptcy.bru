meta {
  name: Declare Bankruptcy
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/bankruptcy
  body: none
  auth: none
}

headers {
  Origin: {{client_url}}
  Authorization: Bearer {{auth_token}}
}

params:query {
}

docs {
  # Declare Bankruptcy

  Reset credits when the authenticated user is broke and has no pending bets.

  **Endpoint:** `POST /api/bankruptcy`
}

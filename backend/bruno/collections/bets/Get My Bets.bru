meta {
  name: Get My Bets
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/bets/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

headers {
  Origin: {{client_url}}
}

params:query {
}

docs {
  # Get My Bets

  Return the authenticated whale user's most recent bets.

  **Endpoint:** `GET /api/bets/me`
}

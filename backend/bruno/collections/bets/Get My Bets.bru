meta {
  name: Get My Bets
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/bets/me
  body: none
  auth: none
}

headers {
  Origin: {{client_url}}
  Authorization: Bearer {{auth_token}}
}

params:query {
}

docs {
  # Get My Bets

  Return the authenticated user's most recent bets.

  **Endpoint:** `GET /api/bets/me`
}

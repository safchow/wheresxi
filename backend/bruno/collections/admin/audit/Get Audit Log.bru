meta {
  name: Get Audit Log
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/admin/audit
  body: none
  auth: none
}

headers {
  Origin: {{client_url}}
  Authorization: Bearer {{auth_token}}
}

params:query {
}

docs {
  # Get Audit Log

  Return the most recent admin audit entries.

  **Endpoint:** `GET /api/admin/audit`

  Requires an `ADMIN` token.
}

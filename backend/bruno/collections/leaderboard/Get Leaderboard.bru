meta {
  name: Get Leaderboard
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/leaderboard
  body: none
  auth: none
}

headers {
  Origin: {{client_url}}
}

params:query {
}

script:pre-request {
  const token = bru.getEnvVar("auth_token") || bru.getVar("auth_token");

  if (token) {
    req.setHeader("Authorization", `Bearer ${token}`);
  }
}

docs {
  # Get Leaderboard

  Return the lifetime earned-credits leaderboard.

  **Endpoint:** `GET /api/leaderboard`
}
